{
  "name": "banana-slides-desktop",
  "version": "0.3.0",
  "description": "Banana Slides Desktop App",
  "main": "main.js",
  "author": "Anionex",
  "license": "MIT",
  "scripts": {
    "start": "electron .",
    "dev": "cross-env NODE_ENV=development electron .",
    "build:meta": "node scripts/sync-build-meta.js",
    "build:win": "node scripts/sync-build-meta.js && electron-builder --win",
    "build:mac": "node scripts/sync-build-meta.js && electron-builder --mac",
    "build:all": "node scripts/sync-build-meta.js && electron-builder --win --mac"
  },
  "dependencies": {
    "electron-log": "^5.1.0",
    "semver": "^7.6.0"
  },
  "devDependencies": {
    "cross-env": "^7.0.3",
    "electron": "^33.0.0",
    "electron-builder": "^25.0.0"
  }
}
