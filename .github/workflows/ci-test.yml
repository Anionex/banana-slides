name: 完整测试套件

# 完整测试在以下情况运行：
# 1. Push到main/develop分支
# 2. PR合并后
# 3. PR添加ready-for-test标签
on:
  push:
    branches: [ main, develop ]
  pull_request:
    types: [closed, labeled]
    branches: [ main, develop ]

# 取消同一分支的旧构建
concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  # ================================
  # 后端测试
  # ================================
  backend-unit-test:
    name: 后端单元测试
    runs-on: ubuntu-latest
    # 只在以下情况运行：push、PR合并后、或添加ready-for-test标签
    if: |
      github.event_name == 'push' ||
      (github.event_name == 'pull_request' && github.event.action == 'closed' && github.event.pull_request.merged == true) ||
      (github.event_name == 'pull_request' && github.event.action == 'labeled' && github.event.label.name == 'ready-for-test')
    
    steps:
      - name: 📥 拉取代码
        uses: actions/checkout@v4
      
      - name: 🐍 设置Python环境
        uses: actions/setup-python@v5
        with:
          python-version: '3.10'
          cache: 'pip'
      
      - name: 📦 安装uv包管理器
        run: |
          curl -LsSf https://astral.sh/uv/install.sh | sh
          echo "$HOME/.cargo/bin" >> $GITHUB_PATH
      
      - name: 📦 安装依赖（含测试依赖）
        run: |
          uv sync --extra test
      
      - name: 🧪 运行单元测试
        run: |
          cd backend
          uv run pytest tests/unit -v --cov=. --cov-report=xml --cov-report=term
        continue-on-error: false
      
      - name: 📊 上传覆盖率报告
        uses: codecov/codecov-action@v4
        with:
          file: ./backend/coverage.xml
          flags: backend
          name: backend-coverage

  backend-integration-test:
    name: 后端集成测试
    runs-on: ubuntu-latest
    needs: backend-unit-test
    
    steps:
      - name: 📥 拉取代码
        uses: actions/checkout@v4
      
      - name: 🐍 设置Python环境
        uses: actions/setup-python@v5
        with:
          python-version: '3.10'
      
      - name: 📦 安装uv
        run: |
          curl -LsSf https://astral.sh/uv/install.sh | sh
          echo "$HOME/.cargo/bin" >> $GITHUB_PATH
      
      - name: 📦 安装依赖（含测试依赖）
        run: uv sync --extra test
      
      - name: 🧪 运行集成测试
        run: |
          cd backend
          uv run pytest tests/integration -v
        env:
          # 使用mock模式，不依赖真实API
          TESTING: true
          GOOGLE_API_KEY: ${{ secrets.GOOGLE_API_KEY || 'mock-api-key-for-testing' }}

  # ================================
  # 前端测试
  # ================================
  frontend-test:
    name: 前端测试
    runs-on: ubuntu-latest
    
    steps:
      - name: 📥 拉取代码
        uses: actions/checkout@v4
      
      - name: 📦 设置Node.js环境
        uses: actions/setup-node@v4
        with:
          node-version: '18'
          cache: 'npm'
          cache-dependency-path: frontend/package-lock.json
      
      - name: 📦 安装依赖
        run: |
          cd frontend
          npm ci
      
      - name: 🔍 Lint检查
        run: |
          cd frontend
          npm run lint
      
      - name: 🧪 运行单元测试
        run: |
          cd frontend
          npm test -- --run --coverage
        continue-on-error: false
      
      - name: 📊 上传覆盖率报告
        uses: codecov/codecov-action@v4
        with:
          file: ./frontend/coverage/coverage-final.json
          flags: frontend
          name: frontend-coverage
      
      - name: 🏗️ 构建检查
        run: |
          cd frontend
          npm run build

  # ================================
  # Docker环境测试
  # ================================
  docker-test:
    name: Docker环境测试
    runs-on: ubuntu-latest
    needs: [backend-integration-test, frontend-test]
    
    steps:
      - name: 📥 拉取代码
        uses: actions/checkout@v4
      
      - name: 🔧 设置环境变量
        run: |
          # 使用通用脚本自动处理所有环境变量替换
          chmod +x scripts/setup-env-from-secrets.sh
          ./scripts/setup-env-from-secrets.sh
        env:
          # 导出所有Secrets为环境变量（脚本会自动检测并替换）
          AI_PROVIDER_FORMAT: ${{ secrets.AI_PROVIDER_FORMAT }}
          GOOGLE_API_KEY: ${{ secrets.GOOGLE_API_KEY }}
          GOOGLE_API_BASE: ${{ secrets.GOOGLE_API_BASE }}
          OPENAI_API_KEY: ${{ secrets.OPENAI_API_KEY }}
          OPENAI_API_BASE: ${{ secrets.OPENAI_API_BASE }}
          OPENAI_TIMEOUT: ${{ secrets.OPENAI_TIMEOUT }}
          OPENAI_MAX_RETRIES: ${{ secrets.OPENAI_MAX_RETRIES }}
          TEXT_MODEL: ${{ secrets.TEXT_MODEL }}
          IMAGE_MODEL: ${{ secrets.IMAGE_MODEL }}
          LOG_LEVEL: ${{ secrets.LOG_LEVEL }}
          FLASK_ENV: ${{ secrets.FLASK_ENV }}
          SECRET_KEY: ${{ secrets.SECRET_KEY }}
          PORT: ${{ secrets.PORT }}
          CORS_ORIGINS: ${{ secrets.CORS_ORIGINS }}
          MAX_DESCRIPTION_WORKERS: ${{ secrets.MAX_DESCRIPTION_WORKERS }}
          MAX_IMAGE_WORKERS: ${{ secrets.MAX_IMAGE_WORKERS }}
          MINERU_TOKEN: ${{ secrets.MINERU_TOKEN }}
          MINERU_API_BASE: ${{ secrets.MINERU_API_BASE }}
          IMAGE_CAPTION_MODEL: ${{ secrets.IMAGE_CAPTION_MODEL }}
          OUTPUT_LANGUAGE: ${{ secrets.OUTPUT_LANGUAGE }}
      
      - name: 🐳 构建Docker镜像
        run: |
          docker-compose build --no-cache
      
      - name: 🚀 启动服务
        run: |
          docker-compose up -d
          echo "等待服务就绪..."
          sleep 20
      
      - name: 🏥 健康检查
        run: |
          # 后端健康检查
          echo "检查后端..."
          for i in {1..30}; do
            if curl -f http://localhost:5000/health; then
              echo "✓ 后端启动成功"
              break
            fi
            echo "等待后端... ($i/30)"
            sleep 2
          done
          
          # 前端健康检查
          echo "检查前端..."
          for i in {1..30}; do
            if curl -f http://localhost:3000; then
              echo "✓ 前端启动成功"
              break
            fi
            echo "等待前端... ($i/30)"
            sleep 2
          done
      
      - name: 🧪 运行Docker环境测试
        run: |
          chmod +x scripts/test_docker_environment.sh
          AUTO_CLEANUP=false ./scripts/test_docker_environment.sh
      
      - name: 📋 查看容器日志（失败时）
        if: failure()
        run: |
          echo "=== Backend Logs ==="
          docker-compose logs backend
          echo "=== Frontend Logs ==="
          docker-compose logs frontend
      
      - name: 🧹 清理环境
        if: always()
        run: |
          docker-compose down -v
          docker system prune -f

  # ================================
  # E2E测试
  # ================================
  e2e-test:
    name: 端到端测试
    runs-on: ubuntu-latest
    needs: docker-test
    # 注：完整E2E测试只用Gemini格式
    # OpenAI格式的兼容性在backend-integration-test中验证（等价替换原理）
    
    steps:
      - name: 📥 拉取代码
        uses: actions/checkout@v4
      
      - name: 📦 设置Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '18'
      
      - name: 🎭 安装Playwright
        run: |
          npm install -D @playwright/test
          npx playwright install --with-deps chromium
      
      - name: 🔧 设置环境（使用真实API key）
        run: |
          # 使用通用脚本自动处理所有环境变量替换
          chmod +x scripts/setup-env-from-secrets.sh
          ./scripts/setup-env-from-secrets.sh
          
          # E2E测试使用Gemini格式（完整流程测试）
          echo "🤖 E2E测试使用Gemini格式"
          grep "^AI_PROVIDER_FORMAT=" .env
        env:
          # 导出所有Secrets为环境变量（脚本会自动检测并替换）
          # E2E测试固定使用Gemini格式
          AI_PROVIDER_FORMAT: gemini
          GOOGLE_API_KEY: ${{ secrets.GOOGLE_API_KEY }}
          GOOGLE_API_BASE: ${{ secrets.GOOGLE_API_BASE }}
          OPENAI_API_KEY: ${{ secrets.OPENAI_API_KEY }}
          OPENAI_API_BASE: ${{ secrets.OPENAI_API_BASE }}
          OPENAI_TIMEOUT: ${{ secrets.OPENAI_TIMEOUT }}
          OPENAI_MAX_RETRIES: ${{ secrets.OPENAI_MAX_RETRIES }}
          TEXT_MODEL: ${{ secrets.TEXT_MODEL }}
          IMAGE_MODEL: ${{ secrets.IMAGE_MODEL }}
          LOG_LEVEL: ${{ secrets.LOG_LEVEL }}
          FLASK_ENV: ${{ secrets.FLASK_ENV }}
          SECRET_KEY: ${{ secrets.SECRET_KEY }}
          PORT: ${{ secrets.PORT }}
          CORS_ORIGINS: ${{ secrets.CORS_ORIGINS }}
          MAX_DESCRIPTION_WORKERS: ${{ secrets.MAX_DESCRIPTION_WORKERS }}
          MAX_IMAGE_WORKERS: ${{ secrets.MAX_IMAGE_WORKERS }}
          MINERU_TOKEN: ${{ secrets.MINERU_TOKEN }}
          MINERU_API_BASE: ${{ secrets.MINERU_API_BASE }}
          IMAGE_CAPTION_MODEL: ${{ secrets.IMAGE_CAPTION_MODEL }}
          OUTPUT_LANGUAGE: ${{ secrets.OUTPUT_LANGUAGE }}
      
      - name: 🐳 启动Docker环境
        run: |
          docker-compose up -d
          sleep 25
      
      - name: 🏥 健康检查
        run: |
          # 等待服务完全就绪
          for i in {1..30}; do
            if curl -f http://localhost:5000/health && curl -f http://localhost:3000; then
              echo "✓ 服务就绪"
              break
            fi
            echo "等待服务启动... ($i/30)"
            sleep 2
          done
      
      - name: 🎬 运行基础E2E测试
        run: |
          # 运行基础UI测试（不依赖真实API）
          npx playwright test home.spec.ts create-ppt.spec.ts
        env:
          CI: true
        continue-on-error: false
      
      - name: 🚀 运行完整流程E2E测试（需要真实API）
        if: secrets.GOOGLE_API_KEY != ''
        run: |
          # 运行完整流程测试（从创建到导出PPT）
          # 使用Gemini格式测试完整流程
          echo "🧪 完整E2E测试 - Gemini格式"
          npx playwright test full-flow.spec.ts --workers=1
        env:
          CI: true
        timeout-minutes: 15
        continue-on-error: false
      
      - name: ⚠️ 跳过完整流程测试
        if: secrets.GOOGLE_API_KEY == ''
        run: |
          echo "⚠️ 跳过完整流程E2E测试"
          echo "原因: 未配置 GOOGLE_API_KEY"
          echo "提示: OpenAI格式的兼容性在集成测试中验证"
      
      - name: 📊 上传测试报告
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: playwright-report
          path: playwright-report/
          retention-days: 7
      
      - name: 📸 上传失败截图
        if: failure()
        uses: actions/upload-artifact@v4
        with:
          name: playwright-screenshots
          path: test-results/
          retention-days: 7
      
      - name: 🧹 清理
        if: always()
        run: docker-compose down -v

  # ================================
  # 安全扫描
  # ================================
  security-scan:
    name: 安全扫描
    runs-on: ubuntu-latest
    
    steps:
      - name: 📥 拉取代码
        uses: actions/checkout@v4
      
      - name: 🔍 后端依赖安全扫描
        run: |
          pip install safety
          safety check --json || echo "发现安全漏洞（警告）"
        continue-on-error: true
      
      - name: 🔍 前端依赖安全扫描
        run: |
          cd frontend
          npm audit --audit-level=moderate || echo "发现安全漏洞（警告）"
        continue-on-error: true
      
      - name: 🔒 Dockerfile安全扫描
        uses: hadolint/hadolint-action@v3.1.0
        with:
          dockerfile: backend/Dockerfile
        continue-on-error: true

  # ================================
  # 测试总结
  # ================================
  test-summary:
    name: 📊 测试总结
    runs-on: ubuntu-latest
    needs: [backend-unit-test, backend-integration-test, frontend-test, docker-test, e2e-test, security-scan]
    if: always()
    
    steps:
      - name: 📊 输出测试结果
        run: |
          echo "================================="
          echo "🎯 CI/CD 测试完成"
          echo "================================="
          echo ""
          echo "测试结果："
          echo "  后端单元测试: ${{ needs.backend-unit-test.result }}"
          echo "  后端集成测试: ${{ needs.backend-integration-test.result }}"
          echo "  前端测试: ${{ needs.frontend-test.result }}"
          echo "  Docker环境测试: ${{ needs.docker-test.result }}"
          echo "  E2E测试: ${{ needs.e2e-test.result }}"
          echo "  安全扫描: ${{ needs.security-scan.result }}"
          echo ""
      
      - name: ✅ 检查是否全部通过
        if: |
          needs.backend-unit-test.result != 'success' ||
          needs.backend-integration-test.result != 'success' ||
          needs.frontend-test.result != 'success' ||
          needs.docker-test.result != 'success' ||
          needs.e2e-test.result != 'success'
        run: |
          echo "❌ 部分测试失败，请检查日志"
          exit 1
      
      - name: 🎉 全部测试通过
        if: |
          needs.backend-unit-test.result == 'success' &&
          needs.backend-integration-test.result == 'success' &&
          needs.frontend-test.result == 'success' &&
          needs.docker-test.result == 'success' &&
          needs.e2e-test.result == 'success'
        run: |
          echo "🎉 所有测试通过！可以合并代码"

